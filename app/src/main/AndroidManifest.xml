<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lanproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanProject">
        <activity android:name=".Portal5Activity">
            android:parentActivityName=".MainPageActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPageActivity" />
        </activity>
        <activity android:name=".Portal4Activity">
            android:parentActivityName=".MainPageActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPageActivity" />
        </activity>
        <activity android:name=".Portal3Activity">
            android:parentActivityName=".MainPageActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPageActivity" />
        </activity>
        <activity android:name=".Portal2Activity">
            android:parentActivityName=".MainPageActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPageActivity" />
        </activity>
        <activity android:name=".Portal1Activity">
            android:parentActivityName=".MainPageActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPageActivity" />
        </activity>
        <activity android:name=".MainPageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>